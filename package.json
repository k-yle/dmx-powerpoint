{
  "name": "dmx-powerpoint",
  "version": "2.0.0",
  "description": "💡 🎚️ Control powerpoint from a lighting console (via sACN)",
  "author": "Kyle Hensel",
  "license": "SEE LICENSE IN LICENSE",
  "main": "dist/electron",
  "scripts": {
    "start:cra": "react-scripts start",
    "start:electron": "electron . --dev",
    "start": "yarn build:tsc && concurrently -k -c blue,green \"npm:start:electron\" \"npm:start:cra\"",
    "build:cra": "react-scripts build && mv -f build dist/client",
    "build:tsc": "tsc --build config",
    "build:electron": "electron-builder --config config/electron-builder.yml",
    "build": "yarn clean && yarn build:tsc && yarn build:cra && yarn build:electron",
    "lint": "eslint --ext .js,.ts,.tsx --ignore-path .gitignore .",
    "clean": "rm -rf dist build"
  },
  "dependencies": {
    "@fluentui/react": "^7.121.2",
    "menubar": "^9.0.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.1",
    "sacn": "^3.2.0",
    "slideshow": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "12.12.6",
    "@types/react": "^16.9.41",
    "@types/react-dom": "^16.9.8",
    "concurrently": "^5.2.0",
    "electron": "^9.0.5",
    "electron-builder": "^22.7.0",
    "eslint": "^6.6.0",
    "eslint-config-kyle": "^1.0.41",
    "typescript": "^3.9.5"
  },
  "engines": {
    "node": "~12",
    "npm": "please-use-yarn"
  },
  "eslintConfig": {
    "extends": "kyle"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
